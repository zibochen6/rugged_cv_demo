[pytest]
# Single test root. `--import-mode=importlib` keeps same-named test modules
# (e.g. several test_state_machine.py) from colliding during collection.
testpaths = tests
addopts = --import-mode=importlib